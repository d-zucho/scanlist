import { LucideIcon } from 'lucide-react';

export type TNavItem = {
    name: string;
    href: string;
};

export type THowItWorksItem = {
    title: string;
    description: string;
    icon: LucideIcon;
    
};